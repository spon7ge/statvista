import { LeagueSectionSwitcher } from "@/features/home/LeagueSectionSwitcher";

/** Horizontal league pills under the Props title. */
export function PropPicksLeagueSwitcher() {
  return <LeagueSectionSwitcher section="Props" />;
}
