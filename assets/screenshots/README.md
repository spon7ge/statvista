# Screenshot assets for README

PNG captures referenced from the root `README.md`. Prefer PNG over SVG for real UI screenshots.

| File | Surface |
|------|---------|
| `mlb-games.png` | MLB Games — live now + rest of slate (`/` → `/mlb/matchups`) |
| `mlb-props.png` | MLB Props board — filters + DFS / sportsbook table |
| `mlb-game-center.png` | MLB game detail — live matchup, pitch zone, box, game flow |

Unused leftovers (not linked from the root README): `home.png`, `wnba-game-center.png`, and older SVG placeholders (`architecture-pipeline.svg`, `dashboard-*.svg`, `api-docs.svg`, `dbt-lineage.svg`).

Primary product walkthrough video lives in [`assets/demo/`](../demo/).
